# Casino card

This project is test task for Moonshot

### Run
To run project

- Install dependencies if not installed
  ```shell
  npm install
  ```
  
- Run
  ```shell
  npm run start
  ```

### Run
To run project

- Install dependencies if not installed
  ```shell
  npm install
  ```
  
- Run
  ```shell
  npm run build
  ```
